import styled, * as othersFeatures from "styled-components";
import View from "./lib/index.js";
const styledComponents = { styled, others: othersFeatures };

export { styledComponents };
export default View;
